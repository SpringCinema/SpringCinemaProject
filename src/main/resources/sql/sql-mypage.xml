<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.esc.springcinema.mapper.MypageMapper">
    <!-- 마이페이지 영화 예약목록을 가져오기(예약중, 취소된거 선택가능) -->
    <select id="selectBookList" parameterType="String" resultType="com.esc.springcinema.dto.BooksDto">
        SELECT book_num, reserve_date, title, seat_code, cinema_name,
               screen_hall_name, view_time, adult_yn FROM books WHERE id = #{userId} AND state_yn = #{state};
    </select>

    <!-- 내 정보 불러오는 쿼리 -->
    <select id="selectMyInfo" resultType="com.esc.springcinema.dto.MemberDto">
        SELECT * FROM members WHERE id = #{id}
    </select>
</mapper>